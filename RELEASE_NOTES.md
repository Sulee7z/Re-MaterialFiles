# Re-MaterialFiles 1.8.4

## 🚨 修复

- 修复文件操作（复制/移动/删除等）完成后可能崩溃的问题（`Cannot invoke setValue on a background thread`：抽屉导航 LiveData 在后台线程被首次初始化导致；现已将刷新调度到主线程执行）。

## 🖱️ 全局拖拽与双栏体验

- 全局拖放：拖动文件到文件夹行上直接移动（仅同栏生效；跨栏拖动仍按原有"移动到对面栏"处理），拖动中目标文件夹高亮。
- 拖动到顶部/底部边缘自动滚动：长列表拖动文件靠近列表边缘时自动滚动，占位视图方案在双栏与单栏下均可用。
- 拖动到删除栏：单栏模式可将文件拖到底部删除栏删除（回收站 + 可撤销 Snackbar）。
- 拖动到面包屑：拖到面包屑任一路径段移动到该目录（单栏/双栏均支持）。
- 双栏共享快速滚动条：右缘统一滚动条，点按即跳，哪一栏获得焦点就跟随哪一栏。
- 抽屉打开时暂停分隔条调整，避免打开导航抽屉时误拖栏宽。
- 双栏文件名跑马灯滚动：名称过长时自动横向滚动显示完整名称。

## 📁 文件夹大小与滚动

- 修复授予"所有文件访问"权限后文件夹大小不显示/显示为 0 的问题（改用与文件列表一致的 NOFOLLOW_LINKS 读取方式，遍历改为迭代式、限量 30 万条目，未知大小不再显示为 0 B）。
- 刷新列表后保持原滚动位置；"长文件名换行"等设置变更后同样不再跳回顶部。
- 权限弹窗去重：双栏模式下仅主栏发起权限请求，不再弹两次。

## 🗃️ 此前 hotfix 已包含（相对 v1.8.3 初版）

- 双栏复制/粘贴底栏显示修复。
- 双栏模式：图标大小与布局细节调整、空白处长按新建文件夹、活动栏跟随抽屉、网格列数按栏宽计算、返回键与快捷键仅作用于活动栏。
- Everything 服务器改走 HTTP API（无需 ETP/FTP 即可浏览/搜索/下载）。
- 文档提供器：本应用作为根出现在系统选择器侧边栏（root 模式可选 /data 等受限目录文件）。
- 回收站（删除进回收站 + Snackbar 撤销）、移动后撤销。
- 远程文件系统删除改为永久删除而非回收站。
- SMB 编辑会话修复、SMB 加密协商、SFTP 主机密钥验证、WebDAV 集合 DELETE 兼容 nginx。
- 解压目标编辑、解压到此处、tar.gz 创建支持、压缩包内嵌套归档原位打开与手动保存编辑。
- 无后缀/未知扩展名文件按文件头内容嗅探自动路由打开。
