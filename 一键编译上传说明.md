# 一键上传 GitHub Actions 编译（零配置）

解压后，把整个工程传到你自己的 GitHub 仓库，就会**自动编译**出一个可直接安装的
APK（用 Android 调试密钥自动签名，无需任何 Secrets / 密钥配置）。

对应的工作流文件：`.github/workflows/build-debug.yml`（名字叫「一键编译 Debug APK」）。

---

## 最简单：双击自带的上传脚本（推荐）

工程根目录已附带一键上传脚本（文件名用全大写英文 `UPLOAD`，避免中文名在
Windows 解压后变乱码而找不到），双击运行、按提示走完即可自动建仓库并推送：

- **Windows**：双击 **`UPLOAD.bat`**
- **macOS**：双击 **`UPLOAD.command`**（若提示无法打开，右键 → 打开）
- **Linux**：终端执行 `bash UPLOAD.command`

脚本会自动完成：初始化本地仓库 → 提交 → 创建 GitHub 仓库 → 推送。
它会**优先使用 GitHub CLI（`gh`）** 全自动建仓库；如果没装 `gh`，会让你把
网页上新建的空仓库地址粘进去，其余照样自动完成。

> 前提：电脑装了 **Git**（Windows 到 https://git-scm.com/download/win 安装）。
> 想全自动免手动建仓库，可再装 **GitHub CLI**：https://cli.github.com/ 。
> 首次推送若弹出浏览器要求登录 GitHub 授权，点同意即可。

推送成功后，直接跳到下面第 **3、4** 步去 Actions 里下载 APK。

---

## 手动步骤（不想用脚本时）

### 1. 新建一个空仓库
在 GitHub 点右上角 **+ → New repository**，随便起个名字（公开/私有都行），
**不要**勾选任何初始化选项（README/.gitignore 都不要），点 Create。

### 2. 把工程推上去（二选一）

**方式 A：命令行（推荐，最省事）**
在解压出来的工程目录里执行：
```bash
git init
git add .
git commit -m "init"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

**方式 B：网页上传**
在仓库页面点 **Add file → Upload files**，把解压后**文件夹里的全部内容**拖进去。
> ⚠️ 一定要确保隐藏的 **`.github` 文件夹**也传上去了（里面是编译脚本）。
> macOS/Windows 默认隐藏以 `.` 开头的文件夹，网页拖拽容易漏掉它。
> 如果不确定，用方式 A 的命令行最稳妥。

### 3. 等待自动编译
推送成功后，打开仓库的 **Actions** 标签页，会看到「一键编译 Debug APK」正在运行
（首次进入 Actions 若提示是否启用，点绿色按钮 **I understand… enable** 即可）。
也可以在该页点 **Run workflow** 手动触发。

约 5～10 分钟后跑完（绿色 ✓）。

### 4. 下载 APK
点进那次运行，拉到页面**最底部 Artifacts**，下载
`MaterialFiles.Sora-Editor-debug@xxxx`，解压得到 `.apk`，传到手机安装即可。

---

## 常见问题

- **要不要花钱？** 公开仓库的 Actions 免费；私有仓库每月也有免费额度，个人小项目够用。
- **编译失败红叉？** 点进日志看报错。多数是网络波动导致依赖下载失败，
  点 **Re-run jobs** 重试一次通常就好。
- **想出正式签名的 Release 包？** 用另一个工作流
  `.github/workflows/Sora-Editor.yml`：先到仓库 **Settings → Secrets and variables →
  Actions** 配置 `KEY_STORE`（密钥库 base64）、`KEY_STORE_PASSWORD`、`ALIAS`，
  再到 Actions 里手动运行它。
- **调试包 vs 正式包**：调试包能直接装、能覆盖安装调试包，功能完全一样；
  只是签名不同，不能和正式签名的版本互相覆盖升级。

---

## 说明：为什么用 Debug 版做「一键」
Release 版需要你自己的签名密钥（否则打包出来是未签名的、装不了），所以它保留为
**手动触发**。而 Debug 版用系统自带的调试密钥自动签名，**不需要任何配置**，
最适合「解压即传、传完就有包」。两个工作流都在，按需选用。
